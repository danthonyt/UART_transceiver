ref model mismatches
-ref model sees !empty immediately when uart rx gets a byte
    -DUT has latency -- still looks empty when ref model thinks !empty
-an element is written to the tx fifo
    - uart tx pulls byte and starts sending
    - axil writes a tx fifo reset command
    - tx fifo cleared - ref model clears tx fifo
    - uart tx finishes
    - ref model pulls expected from empty tx fifo
    - mismatch
    - RESULT: 
